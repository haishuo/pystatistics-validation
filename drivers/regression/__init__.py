"""Regression validation drivers (OLS + GLM families) built on ``pystatsval``.

Generators that fit pystatistics and the R reference on canonical datasets and
freeze ``validation-run/v1`` artifacts under ``artifacts/regression/``. They
consume a PyPI-installed pystatistics; they never modify the library.
"""
