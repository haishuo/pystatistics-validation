<!--
CANONICAL REPORT STRUCTURE — this file documents the sections render_report.py emits.
It is NOT rendered directly; render/report.py mirrors it. Numbers come from
artifacts/<subsystem>/v<version>/ ; prose comes from subsystems/<subsystem>/meta.json.
Never hand-edit numbers into a report — fix the artifact or the renderer and re-render.

Placeholders: {{subsystem}} {{version}} {{...}}. The seven questions every report
answers (see ARCHITECTURE.md, principle 5) are mapped to sections below.
-->

# Validation report — pystatistics `{{subsystem}}` v{{version}}

> This report validates **pystatistics {{subsystem}} v{{version}}**, installed from
> PyPI. Evidence: `artifacts/{{subsystem}}/v{{version}}/`. Generated by
> `render_report.py` from frozen artifacts — do not edit numbers by hand.

## 1. What statistical procedure is implemented?
{{procedure}}

## 2. Which algorithms are used?
{{algorithms}}

## 3. How does it compare to R / SAS / another reference?
{{reference}}
{{agreement_tables}}

## 4. What numerical tolerances are expected?
{{tolerances}}

## 5. What benchmarks were run?
{{benchmark_tables}}

## 6. What are the known limitations?
{{limitations}}

## 7. Which version of pystatistics do these results apply to?
- **Library version:** {{version}}  ·  **Install source:** {{install_source}}
- **Frozen:** {{frozen_utc}}  ·  **Reference:** {{reference_name}}
- **Provenance:** {{provenance}}
- **Hosts / devices:** {{hosts}}

---
_Rendered {{rendered_utc}} from manifest `{{manifest_path}}`._
