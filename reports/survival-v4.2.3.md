# Validation report — pystatistics `survival` v4.2.3

> This report validates **pystatistics survival v4.2.3**, installed from **pypi**. Evidence lives in `artifacts/survival/v4.2.3/`. It was generated by `render_report.py` from frozen artifacts — **do not edit numbers by hand**; fix the artifact or the renderer and re-render.

**Subsystem:** Survival analysis (Kaplan-Meier, log-rank, Cox PH, discrete-time) (`pystatistics.survival`)

## 1. What statistical procedure is implemented?

Time-to-event analysis under one module. kaplan_meier estimates the product-limit survival curve S(t) with Greenwood standard errors and transformed confidence bands. survdiff is the log-rank (and G-rho weighted) test comparing survival across groups, reporting a chi-square statistic, degrees of freedom, observed/expected events per group, and a p-value. coxph fits the Cox proportional-hazards model by Newton-Raphson on the partial likelihood, reporting log hazard ratios, hazard ratios (exp coef), standard errors from the observed information, asymptotic-normal Wald z-statistics, p-values, Harrell's concordance, and the null/model partial log-likelihood. discrete_time fits a discrete-time survival model by expanding the data to person-period (long) format and fitting a logistic regression, reporting covariate hazard ratios and a per-interval baseline hazard.

## 2. Which algorithms are used?

- Kaplan-Meier (kaplan_meier), CPU: the product-limit estimator S(t) = prod(1 - d_j/n_j) over event times, with the Greenwood variance Var(S(t)) = S(t)^2 * sum(d_j / (n_j (n_j - d_j))). Confidence bands via the log (R default), plain, or log-log transformation. fp64, deterministic; a single sorted pass over the data.
- Log-rank / G-rho (survdiff), CPU: at each distinct event time, observed minus expected events per group are accumulated with the Mantel-Haenszel hypergeometric variance; the test statistic is the quadratic form (O-E)' V^-1 (O-E) on the first K-1 groups, referred to chi-square with K-1 df. rho > 0 gives the Fleming-Harrington weighted family (rho=1 is Peto-Peto / Gehan-Wilcoxon). fp64, deterministic.
- Cox proportional hazards (coxph), CPU only: Newton-Raphson on the partial log-likelihood with Efron's (R default) or Breslow's handling of tied event times. Each iteration computes the score vector and observed information by sweeping the risk sets; standard errors are sqrt of the diagonal of the inverted information at the solution; Wald z = coef/se referred to the normal. Step-halving caps the Newton step to prevent exp-overflow. Harrell's concordance is computed from all comparable pairs. fp64, deterministic.
- Discrete-time survival (discrete_time): the data are expanded to person-period format (one row per subject per interval at risk), then a binomial GLM is fit with interval indicators (the baseline hazard) plus covariates. The logistic fit is delegated to pystatistics.regression.fit(family='binomial'), so it inherits regression's CPU and GPU backends; covariate hazard ratios are exp(coef) and the per-interval baseline hazard is expit(interval coef).
- Backends: kaplan_meier, survdiff, and coxph are CPU-only and expose no backend= parameter (see the GPU reconciliation below). Only discrete_time takes backend= ('cpu' default, 'gpu', 'gpu_fp64' [CUDA-only exact path], or 'auto'), which it forwards to the regression binomial GLM that does the actual fitting.

## 3. How does it compare to R survival::survfit / survdiff / coxph?

The R survival package (Therneau) is the gold-standard reference for time-to-event analysis. The head-to-head pulls survfit() for the Kaplan-Meier curve, survdiff() for the log-rank test, and coxph() for the proportional-hazards fit, all on the canonical survival::lung dataset (NCCTG advanced lung cancer; Loprinzi et al. 1994). Discrete-time survival has no single R reference function, so it is validated against R's glm(family=binomial) fit on the identical person-period design that pystatistics constructs internally (the design is reconstructed in the driver and guarded: the reconstructed row count must equal the library's person_period_n). Agreement in the survival curve, the test statistic, the coefficient/HR/SE/z/p vectors, the concordance, and the partial log-likelihood is the correctness claim.


**Correctness vs R survival — Powerhouse (CPU / fp64), per quantity**

| procedure | quantity | n_elements | max_abs | max_rel |
| --- | --- | --- | --- | --- |
| kaplan_meier | time | 139 | 0 | 0 |
| kaplan_meier | survival | 139 | 2.22e-16 | 7.197e-16 |
| kaplan_meier | n_risk | 139 | 0 | 0 |
| kaplan_meier | std_err | 139 | 2.082e-17 | 7.592e-16 |
| kaplan_meier | ci_lower | 139 | 2.22e-16 | 1.006e-15 |
| kaplan_meier | ci_upper | 139 | 3.331e-16 | 1.129e-15 |
| kaplan_meier | median_survival | 1 | 0 | 0 |
| survdiff | statistic | 1 | 1.776e-15 | 1.72e-16 |
| survdiff | p_value | 1 | 2.299e-17 | 1.753e-14 |
| survdiff | observed | 2 | 0 | 0 |
| survdiff | expected | 2 | 1.421e-14 | 1.552e-16 |
| coxph | coefficients | 3 | 1.998e-15 | 8.465e-15 |
| coxph | hazard_ratios | 3 | 1.554e-15 | 1.929e-15 |
| coxph | standard_errors | 3 | 6.384e-16 | 2.733e-14 |
| coxph | z_values | 3 | 2.265e-14 | 1.897e-14 |
| coxph | p_values | 3 | 8.91e-15 | 3.833e-14 |
| coxph | concordance | 1 | 8.684e-05 | 0.0001363 |
| coxph | loglik_model | 1 | 1.137e-13 | 1.559e-16 |
| discrete_time | coefficients | 3 | 2.22e-16 | 1.03e-15 |
| discrete_time | standard_errors | 3 | 5.551e-17 | 2.67e-16 |
| discrete_time | z_values | 3 | 1.11e-15 | 1.059e-15 |
| discrete_time | p_values | 3 | 6.661e-16 | 5.997e-15 |


**Discrete-time CPU vs R on real flchain (~83k person-period rows), per quantity**

| dataset | person_period_n | quantity | n_elements | max_abs | max_rel |
| --- | --- | --- | --- | --- | --- |
| flchain_yearly | 8.292e+04 | coefficients | 7 | 1.11e-15 | 8.11e-15 |
| flchain_yearly | 8.292e+04 | standard_errors | 7 | 1.11e-16 | 8.899e-16 |
| flchain_yearly | 8.292e+04 | z_values | 7 | 3.553e-14 | 7.627e-15 |
| flchain_yearly | 8.292e+04 | p_values | 7 | 3.83e-15 | 6.284e-14 |


## 4. What numerical tolerances are expected?

- **km_vs_r:** Kaplan-Meier matches R survfit() to fp64 round-off: event times and at-risk counts are bit-identical, and survival, Greenwood standard error, and the log-transformed confidence bands agree to ~1e-15 relative. The comparison is restricted to event times (where R's summary reports rows), the same set pystatistics emits.
- **logrank_vs_r:** The log-rank chi-square statistic and its p-value match R survdiff() to ~1e-14; observed events are integer-identical and expected events agree to ~1e-14. (Default rho=0 standard log-rank; the G-rho family is implemented but not part of the lung head-to-head.)
- **coxph_vs_r:** Cox PH coefficients, hazard ratios, standard errors, Wald z-statistics, p-values, and the model partial log-likelihood match R coxph() (ties='efron') to fp64 round-off (~1e-13 to 1e-15 relative) on the lung age+sex+ph.ecog model.
- **coxph_concordance:** Harrell's concordance agrees with R only to ~1e-4, NOT to round-off. This is a definitional difference, not a defect: pystatistics computes the classic Harrell C from comparable pairs, while R's survival >= 3.x reports a tie-aware concordance (Therneau's concordancefit, which counts time ties and is computed via an O(n log n) balanced-tree algorithm). The two definitions differ at the fourth decimal on data with tied times; both are valid C-statistics.
- **ties_efron_vs_breslow:** Efron (the default, matching R) and Breslow are both implemented for tied event times. The validation pins ties='efron' on both sides; Efron is the more accurate approximation and R's default. Breslow agreement is not separately tabulated here.
- **discrete_vs_r:** Discrete-time covariate coefficients, standard errors, Wald z-statistics, and p-values match R glm(binomial) on the identical person-period design to fp64 round-off (~1e-14). This holds both on the small lung design (coarse quantile-binned intervals; see limitations) and on the large real flchain person-period design (yearly bins, ~83k rows): coefficients ~1e-13, standard errors ~1e-15, z ~1e-13, p ~1e-12 — fp64 agreement at genuine scale.
- **discrete_gpu_fp64:** On CUDA, discrete_time(backend='gpu_fp64') is the exact double-precision GPU path: covariate coefficients match the CPU fp64 fit to ~1e-15 (numerically exact) across the bin-granularity sweep, including the large monthly design (~1M person-period rows). This is the robust large-scale GPU path.
- **discrete_gpu_fp32:** The float32 GPU path (backend='gpu') is judged at the float32 tier by the relative Newton decrement (RIGOR R9: never against R's absolute fp64 criterion). 4.2.3 fixed a premature-stop FALSE NEGATIVE: the float32 IRLS loop used to halt at the float32 round-off floor (eff_tol = max(tol, 2*sqrt(n)*eps_fp32)) while slowly-converging low-baseline-hazard interval dummies were still moving, so the strict gate saw a non-stationary iterate and spuriously rejected fits that float32 can in fact reach. With the decrement-based stop plus monotone-descent step-halving (4.2.3), the float32 path iterates to genuine stationarity. Result: on CUDA the float32 fit now converges across the ENTIRE flchain person-period sweep (yearly through monthly, ~1M rows), covariate coefficients matching the CPU fp64 fit to ~1e-5. On Apple Silicon (MPS, no float64) the same false negative is fixed and most grids converge to ~1e-5 (5yr, 2.5yr, yearly, half-year, monthly in the frozen run), BUT MPS float32 remains intermittently fragile: at some person-period sizes the float32 XtWX is not positive-definite and the inner Cholesky breaks down, failing loud (A6-correct -- a genuine float32 limitation, conditioning- and run-dependent; e.g. quarterly broke down in the frozen run while yearly and monthly converged). The robust, deterministic GPU path at all scales is CUDA: float32 across the whole sweep, or gpu_fp64 for an exact (~1e-13) fit.

## Constitutional compliance & GPU backend reconciliation

This validation tracks the survival module against the PyStatistics Constitution (CONVENTIONS.md). The 4.0.0 audit surfaced six findings (C1-C3 API-shape, O1-O2 Cox performance, and a GPU convergence issue uncovered while probing discrete-time on GPU); 4.2.1 closes the last conf_int gap (C4) and adds direct GPU validation of the discrete-time path on real data. All actionable findings were fixed across 4.1.0, 4.2.0, and 4.2.1; the status of each is recorded below. The numerical results are unaffected by any of these changes.

**GPU backend reconciliation.** The constitution lists survival.coxph as a deliberate NO-GPU module (the partial likelihood is inherently sequential). The public API honors this: kaplan_meier, survdiff, and coxph expose no backend= parameter; KM and log-rank are cheap one-pass reductions and Cox PH is O(n log n) on the CPU (4.2.0). The one GPU-capable path is discrete_time, which forwards backend= to pystatistics.regression.fit(family='binomial') over the person-period expansion. Two releases since the 4.0.0 audit reshaped its performance and reliability, both validated here directly on the real survival::flchain person-period design. (1) 4.2.2 rewrote the CPU least-squares core (qr_solve) from full column-pivoted QR to a single-pass rank-revealing QR equivalent to R's lm.fit/glm.fit, so the CPU discrete-time fit now MEETS OR BEATS R at scale (monthly ~1M-row design: 58s vs R glm 71s) with correctness unchanged (~1e-15). (2) 4.2.3 fixed a float32 GPU premature-stop FALSE NEGATIVE (see finding G3): the float32 IRLS loop stopped at the float32 round-off floor while slow low-hazard interval dummies were still moving, so the strict Newton-decrement gate spuriously rejected float32-reachable fits. Replacing the stop with a decrement-based criterion (plus descent step-halving), with the strict acceptance gate untouched, makes the float32 path iterate to genuine stationarity. After both fixes: CUDA float32 converges across the entire flchain sweep including monthly (~1e-5 vs CPU), CUDA gpu_fp64 is exact (~1e-13) -- the robust large-scale GPU path, ~9.5x faster than the (now faster) CPU at monthly and ~18x in float32. Apple Silicon (MPS) float32 has the false negative fixed too and converges on most grids, but remains intermittently fragile (genuine float32 Cholesky breakdowns at some sizes -> fail loud, A6-correct). The A6 contract holds throughout: an explicit request is honored exactly or fails loud -- never a silent CPU/precision fallback, and (since 4.2.3) never a false refusal of a float32-reachable fit. To our knowledge no prior implementation has put discrete-time survival on a GPU at this scale.

**Findings:**

- **C1 — Coefficient solutions lacked the uniform .conf_int accessor** _(RESOLVED in 4.1.0)_: 4.1.0 added a library-wide .conf_int (Wald) + conf_level= across the coefficient models, including CoxSolution; it matches R's summary.coxph hazard-ratio intervals to ~1e-15 and removed the hardcoded 1.96 from the Cox summary. (DiscreteTimeSolution still lacks .conf_int — a minor remaining gap, tracked separately.)
- **C2 — KMSolution used .se / .ci_lower / .ci_upper rather than the uniform names** _(RESOLVED in 4.2.0)_: 4.2.0 added KMSolution.standard_errors (Greenwood SE) and KMSolution.conf_int (the (m,2) [lower,upper] band), matching the library-wide accessor names. The legacy .se/.ci_lower/.ci_upper remain (additive).
- **C3 — Stratified KM/Cox raised the builtin NotImplementedError** _(RESOLVED in 4.2.0)_: 4.2.0 added NotImplementedFeatureError (subclasses both PyStatisticsError and the builtin NotImplementedError); stratified KM/Cox now raise it, routing every survival error through core.exceptions while staying catchable as NotImplementedError.
- **O1 — Harrell's concordance was an O(n^2) nested Python loop** _(RESOLVED in 4.2.0)_: 4.2.0 replaced the all-pairs double loop with an O(n log n) Fenwick-tree count, reproducing the existing Harrell definition exactly. See the scaling study: the pystatistics/R Cox wall ratio went from growing 23x->812x to flat ~1-2.6x.
- **O2 — Cox partial-likelihood/score/information recomputed risk sets per event time** _(RESOLVED in 4.2.0)_: 4.2.0 replaced the per-event-time risk-mask recomputation with vectorized reverse-cumulative-sum risk sets (S0/S1/S2) indexed via searchsorted — one O(n) sweep per Newton iteration. Together with O1 this makes Cox PH O(n log n), matching R; a full fit at n=8000 dropped from ~10s to ~31ms. Results bit-for-bit unchanged.
- **G1 — GPU GLM falsely rejected correct float32 fits (discrete-time on GPU)** _(RESOLVED in 4.2.0)_: Probing discrete_time on GPU exposed a GPU GLM convergence gate that held the strict float64 tolerance on the float32 path: a correct, well-conditioned float32 fit that reached the float32 floor was declared non-converged and failed loud (most often on MPS). The fit was correct (matched CPU to the float32 tier). 4.2.0 decides float32 acceptance by the relative Newton decrement (a stationarity test) instead; genuinely-unreliable fits still fail loud, with no silent CPU/precision fallback. Verified on MPS and CUDA across binomial/Poisson/Gamma.
- **C4 — DiscreteTimeSolution lacked the uniform .conf_int accessor** _(RESOLVED in 4.2.1)_: The 4.1.0 conf_int rollout (C1) left DiscreteTimeSolution as the one coefficient model without .conf_int (tracked as a minor remaining gap). 4.2.1 adds DiscreteTimeSolution.conf_int + .conf_level and discrete_time(conf_level=); the Wald .conf_int surface is now uniform across every coefficient model in the library. Additive, backward-compatible.
- **G2 — Discrete-time GPU path validated directly on real data** _(VALIDATED (4.2.1); mechanism corrected in 4.2.3)_: The discrete-time GPU path is validated directly on the real survival::flchain person-period design (not only inherited from the regression GPU backend). The float32 path initially appeared to fail at moderate scale; that was first attributed to noisy float32 reductions, then to initialization -- both WRONG. The correct diagnosis (a premature stop) and its fix are recorded in G3. CUDA gpu_fp64 is exact at every granularity.
- **O3 — CPU least-squares core rewritten to a single-pass rank-revealing QR (R-parity)** _(RESOLVED in 4.2.2)_: The CPU OLS/GLM solver (core qr_solve) used full column-pivoted QR (LAPACK dgeqp3) on every solve -- heavier than R's lighter rank-revealing QR (LINPACK dqrls), so CPU fits were slower than R for no benefit. 4.2.2 replaced it with a single-pass rank-revealing QR equivalent to R's, reaching speed parity-or-better with R on full-rank AND rank-deficient designs (CONVENTIONS Prime Directive: CPU parity with R is a requirement). For survival this makes the GLM-backed discrete-time CPU fit meet/beat R at scale (monthly 58s vs R 71s); coefficients unchanged (~1e-15). Cox PH is unaffected (it uses a Newton solve on the information matrix, not qr_solve).
- **G3 — float32 GLM IRLS spuriously failed to converge (premature-stop false negative)** _(RESOLVED in 4.2.3)_: On float32 GPU paths the binomial IRLS loop stopped at the float32 round-off floor (eff_tol ~ 2*sqrt(n)*eps_fp32) at ~iteration 5, while slowly-converging low-baseline-hazard interval dummies were still moving ~1 logit/iteration; the strict Newton-decrement acceptance gate then correctly saw a non-stationary iterate and FAILED LOUD -- a false negative, since float32 can reach the fit (the Newton decrement falls to ~1e-12 if the loop continues). Earlier guesses (noisy reductions; initialization) were ruled out by measurement: MPS forms XtWX to ~3e-6, the solve to ~4e-6, and a step from the optimum to ~2e-5 -- the arithmetic is sound; the STOP CRITERION was the defect. 4.2.3 replaces the float32 stop with the relative Newton decrement (the same measure the gate uses) plus monotone-descent step-halving; the strict fp64 acceptance gate is unchanged (no new silently-accepted fits). CUDA float32 now converges across the full flchain sweep including monthly; MPS float32 converges on most grids but stays intermittently fragile (genuine float32 Cholesky breakdowns at some sizes still fail loud, A6-correct). The genuinely float32-infeasible regime (where cond*eps_fp32 exceeds the gate) still fails loud on every backend -- gpu_fp64/CPU are the answer there.

## 5. What benchmarks were run?

### Wall-clock vs R on survival::lung — Powerhouse (CPU / fp64)
_claim: speed · device: cpu · host: powerhouse+r_

| procedure | dataset | n | n_events | wall_pystat_s | wall_r_s | speedup_vs_r |
| --- | --- | --- | --- | --- | --- | --- |
| kaplan_meier | lung_km | 228 | 165 | 0.0001762 | 0.0006 | 3.4059 |
| survdiff | lung_km | 228 |  | 0.0005133 | 0.0008 | 1.5584 |
| coxph | lung_coxph | 227 | 164 | 0.0026 | 0.0012 | 0.4566 |
| discrete_time | lung_discrete | 227 | 164 | 0.0012 | 0.0016 | 1.3178 |

> Per-fit median over timed repeats. With the 4.2.0 O(n log n) Cox PH, all four procedures are within a small constant factor of R on the tiny lung dataset; the scaling studies below carry the asymptotic and at-scale claims.

### Cox PH CPU scaling vs R — Powerhouse (CPU / fp64)
_claim: speed · device: cpu · host: powerhouse+r_

| n | p | n_events | n_iter | wall_pystat_s | wall_r_s | ratio_pystat_over_r |
| --- | --- | --- | --- | --- | --- | --- |
| 250 | 3 | 180 | 4 | 0.0013 | 0.0012 | 1.1224 |
| 500 | 3 | 334 | 3 | 0.002 | 0.0016 | 1.2801 |
| 1000 | 3 | 672 | 3 | 0.0036 | 0.0022 | 1.6573 |
| 2000 | 3 | 1357 | 3 | 0.0075 | 0.0038 | 1.9694 |
| 4000 | 3 | 2628 | 3 | 0.0147 | 0.0064 | 2.3047 |
| 8000 | 3 | 5445 | 3 | 0.0319 | 0.013 | 2.4568 |

> Synthetic proportional-hazards DGP. pystatistics Cox PH is O(n log n) (vectorized reverse-cumsum risk sets + a Fenwick-tree concordance), matching R's complexity: the pystatistics/R wall ratio is FLAT at ~1-2.6x across n=250..8000.

### Discrete-time GPU fit scaling — Powerhouse: CPU (fp64) vs MPS (fp32)
_claim: speed · device: cpu, mps · host: powerhouse_

| problem | p | n | wall cpu (s) | wall acc (s) | speedup | mem cpu (MB) | mem acc (MB) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| flchain_2_5yr | 13 | 3.602e+04 | 0.0347 | 0.0302 | 1.2x | 48.3 | 42.4 |
| flchain_5yr | 10 | 1.969e+04 | 0.0164 | 0.0183 | 0.9x | 29.8 | 8.8 |
| flchain_monthly | 172 | 9.52e+05 | 59.2033 | 2.2463 | 26.4x | 5616 | 2073 |
| flchain_quarterly | 62 | 3.195e+05 | 3.3432 |  |  | 988.3 |  |
| flchain_yearly | 21 | 8.292e+04 | 0.137 | 0.1267 | 1.1x | 80.2 | 46.5 |
| synth_pp_n1040000 | 68 | 1.04e+06 | 10.4226 |  |  |  |  |
| synth_pp_n130000 | 68 | 1.3e+05 | 1.0635 | 0.1854 | 5.7x |  | 1154 |
| synth_pp_n2080000 | 68 | 2.08e+06 | 21.1022 | 1.9252 | 11.0x | 2749 | 2001 |
| synth_pp_n260000 | 68 | 2.6e+05 | 2.36 | 0.2828 | 8.3x |  | 1162 |
| synth_pp_n520000 | 68 | 5.2e+05 | 4.9014 |  |  |  |  |

> Wall-clock of the binomial GLM fit on the person-period design (expansion excluded). As of 4.2.3 MPS float32 converges on most flchain grids (the 4.2.2/4.2.3 premature-stop false-negative is fixed); blank acc cells are genuine intermittent float32 Cholesky breakdowns (XtWX not positive-definite in float32) that fail loud, A6-correct. The robust GPU path is CUDA (see the Forge studies); MPS is the bonus path.

### Discrete-time float32 acceptance boundary — Powerhouse (MPS): accurate when accepted, fails loud when wrong
_claim: stability · device: cpu, mps · host: powerhouse_

| dataset | person_period_n | cols | n_intervals | backend | precision | gate_result | coef_max_rel | forced_max_rel | faithfulness_rel | note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| flchain_5yr | 1.969e+04 | 10 | 3 | gpu | fp32 | accepted | 9.01e-06 |  | 0 |  |
| flchain_2_5yr | 3.602e+04 | 13 | 6 | gpu | fp32 | accepted | 3.194e-05 |  | 0 |  |
| flchain_yearly | 8.292e+04 | 21 | 14 | gpu | fp32 | accepted | 2.373e-05 |  | 0 |  |
| flchain_half_yr | 1.624e+05 | 35 | 28 | gpu | fp32 | accepted | 8.392e-05 |  | 0 |  |
| flchain_quarterly | 3.195e+05 | 62 | 55 | gpu | fp32 | rejected |  |  | 0 | NumericalError |

> Per bin granularity: the float32 acceptance gate now accepts MPS where the fit reaches stationarity (coef_max_rel ~1e-5 vs CPU). Where MPS still rejects it is a genuine float32 inner-solve breakdown (not the old premature stop) -- conditioning/run-dependent and A6-correct. faithfulness_rel confirms the fit-isolated measurement reproduces the public discrete_time coefficients (~0).

### Expansion vs fit vs end-to-end — Powerhouse (CPU): the GLM fit dominates
_claim: speed · device: cpu · host: powerhouse_

| dataset | person_period_n | cols | expansion_s | fit_cpu_s | e2e_discrete_s | expansion_frac_of_e2e |
| --- | --- | --- | --- | --- | --- | --- |
| flchain_5yr | 1.969e+04 | 10 | 0.0159 | 0.0225 | 0.0391 | 0.4071 |
| flchain_2_5yr | 3.602e+04 | 13 | 0.0297 | 0.0373 | 0.0676 | 0.4399 |
| flchain_yearly | 8.292e+04 | 21 | 0.0692 | 0.1262 | 0.2047 | 0.3382 |
| flchain_quarterly | 3.195e+05 | 62 | 0.289 | 3.3122 | 3.6281 | 0.0797 |
| flchain_monthly | 9.52e+05 | 172 | 1.007 | 58.3986 | 60.2312 | 0.0167 |

> The person-period expansion is a pure-Python double loop but a small fraction of end-to-end at scale (~1.7% at monthly); the GLM fit dominates. As of 4.2.2 the CPU fit itself meets/beats R at scale (monthly 58s vs R glm 71s).

### R glm(binomial) at scale on the flchain person-period design — Powerhouse
_claim: speed · device: cpu · host: powerhouse+r_

| dataset | person_period_n | cols | engine | wall_r_s | status |
| --- | --- | --- | --- | --- | --- |
| flchain_yearly | 8.292e+04 | 21 | r:glm | 0.268 | ok |
| flchain_quarterly | 3.195e+05 | 62 | r:glm | 2.806 | ok |
| flchain_monthly | 9.52e+05 | 172 | r:glm | 71.06 | ok |

> R glm wall climbs into the minutes at ~1M person-period rows (monthly ~71s). As of 4.2.2 the pystatistics CPU fit beats R there (58s); the CUDA gpu_fp64 path does it in ~4s.

### Discrete-time GPU fit scaling — Forge: CPU (fp64) vs CUDA gpu_fp64 (exact)
_claim: speed · device: cpu, cuda · host: forge_

| problem | p | n | wall cpu (s) | wall acc (s) | speedup | mem cpu (MB) | mem acc (MB) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| flchain_2_5yr | 13 | 3.602e+04 | 0.0284 | 0.0091 | 3.1x | 17 | 21.5 |
| flchain_5yr | 10 | 1.969e+04 | 0.0143 | 0.0291 | 0.5x | 11.1 | 14.2 |
| flchain_monthly | 172 | 9.52e+05 | 37.9522 | 3.9985 | 9.5x | 5066 | 3987 |
| flchain_quarterly | 62 | 3.195e+05 | 1.5697 | 0.7228 | 2.2x | 615.2 | 502.7 |
| flchain_yearly | 21 | 8.292e+04 | 0.0854 | 0.0232 | 3.7x | 64.8 | 55.8 |
| synth_pp_n1040000 | 68 | 1.04e+06 | 7.2508 | 1.887 | 3.8x |  | 1757 |
| synth_pp_n130000 | 68 | 1.3e+05 | 0.5223 | 0.236 | 2.2x |  | 227 |
| synth_pp_n2080000 | 68 | 2.08e+06 | 16.0459 | 3.8411 | 4.2x |  | 3503 |
| synth_pp_n260000 | 68 | 2.6e+05 | 1.0739 | 0.4671 | 2.3x |  | 446.4 |
| synth_pp_n520000 | 68 | 5.2e+05 | 2.8645 | 0.9363 | 3.1x |  | 883.2 |

> The robust large-scale path: CUDA gpu_fp64 is exact (~1e-13, see the precision study) and accepted at every granularity including monthly -- ~9.5x faster than the same-host CPU fp64 fit (monthly 38s -> 4.0s). Speedup is smaller than 4.2.1 because the CPU baseline itself got faster (Core-QR, 4.2.2).

### Discrete-time GPU fit scaling — Forge: CPU (fp64) vs CUDA (fp32)
_claim: speed · device: cpu, cuda · host: forge_

| problem | p | n | wall cpu (s) | wall acc (s) | speedup | mem cpu (MB) | mem acc (MB) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| flchain_2_5yr | 13 | 3.602e+04 | 0.0284 | 0.0093 | 3.1x | 17 | 15 |
| flchain_5yr | 10 | 1.969e+04 | 0.0143 | 0.0051 | 2.8x | 11.1 | 11.4 |
| flchain_monthly | 172 | 9.52e+05 | 37.9522 | 2.0926 | 18.1x | 5066 | 1998 |
| flchain_quarterly | 62 | 3.195e+05 | 1.5697 | 0.2778 | 5.7x | 615.2 | 255.3 |
| flchain_yearly | 21 | 8.292e+04 | 0.0854 | 0.0242 | 3.5x | 64.8 | 31.4 |
| synth_pp_n1040000 | 68 | 1.04e+06 | 7.2508 | 0.8651 | 8.4x |  | 882.3 |
| synth_pp_n130000 | 68 | 1.3e+05 | 0.5223 | 0.1035 | 5.0x |  | 117.8 |
| synth_pp_n2080000 | 68 | 2.08e+06 | 16.0459 | 1.7557 | 9.1x |  | 1756 |
| synth_pp_n260000 | 68 | 2.6e+05 | 1.0739 | 0.2136 | 5.0x |  | 227 |
| synth_pp_n520000 | 68 | 5.2e+05 | 2.8645 | 0.42 | 6.8x |  | 445.4 |

> CUDA float32 now converges across the WHOLE sweep including monthly (2.1s, ~18x over CPU) -- at 4.2.1 monthly float32 was the borderline reject; the 4.2.3 decrement-stop fix resolved it. Exact fits at every scale: use gpu_fp64 (above).

### Discrete-time GPU precision — Forge (CUDA): fp32 holds through quarterly, fp64 exact throughout
_claim: stability · device: cuda · host: forge_

| dataset | person_period_n | cols | n_intervals | backend | precision | gate_result | coef_max_rel | forced_max_rel | faithfulness_rel | note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| flchain_5yr | 1.969e+04 | 10 | 3 | gpu | fp32 | accepted | 1.453e-05 |  | 0 |  |
| flchain_5yr | 1.969e+04 | 10 | 3 | gpu_fp64 | fp64 | accepted | 1.202e-13 |  | 0 |  |
| flchain_2_5yr | 3.602e+04 | 13 | 6 | gpu | fp32 | accepted | 1.909e-05 |  | 0 |  |
| flchain_2_5yr | 3.602e+04 | 13 | 6 | gpu_fp64 | fp64 | accepted | 7.834e-14 |  | 0 |  |
| flchain_yearly | 8.292e+04 | 21 | 14 | gpu | fp32 | accepted | 8.861e-06 |  | 0 |  |
| flchain_yearly | 8.292e+04 | 21 | 14 | gpu_fp64 | fp64 | accepted | 3.927e-14 |  | 0 |  |
| flchain_half_yr | 1.624e+05 | 35 | 28 | gpu | fp32 | accepted | 3.326e-05 |  | 0 |  |
| flchain_half_yr | 1.624e+05 | 35 | 28 | gpu_fp64 | fp64 | accepted | 4.473e-14 |  | 0 |  |
| flchain_quarterly | 3.195e+05 | 62 | 55 | gpu | fp32 | accepted | 6.177e-05 |  | 0 |  |
| flchain_quarterly | 3.195e+05 | 62 | 55 | gpu_fp64 | fp64 | accepted | 3.461e-13 |  | 0 |  |

> CUDA float32 (backend='gpu') is accepted and accurate to ~1e-5 vs CPU across the sweep; CUDA gpu_fp64 is exact (~1e-13) at every granularity. Contrast MPS, where float32 is intermittently fragile -- the exact gpu_fp64 path is not.

## 6. What are the known limitations?

- Cox PH is CPU-only by construction — the partial likelihood is inherently sequential and is not a GPU target (a constitutional decision, not a gap). As of 4.2.0 the CPU implementation is O(n log n) (vectorized reverse-cumsum risk sets + a Fenwick-tree concordance), matching R's complexity; the scaling study shows a flat pystatistics/R wall ratio. True O(n) is impossible — the event-time sort is O(n log n), and R is O(n log n) too.
- Harrell's concordance differs from R's tie-aware concordance at ~1e-4 (a definitional difference; see tolerances). Both are valid C-statistics.
- Stratified Kaplan-Meier and stratified Cox PH are not implemented (strata=... raises NotImplementedFeatureError). The validation covers the unstratified models that R's lung examples use.
- Discrete-time survival is validated on coarse quantile-binned intervals (5 bins), not the intervals=None default. On continuous data the default uses every unique event time as its own interval, producing hundreds of single-event intervals that separate perfectly — discrete-time methods are designed for genuinely binned time, so the coarse-interval head-to-head is the meaningful comparison.
- discrete_time is the one GPU-capable survival path (it forwards backend= to the regression binomial GLM over the person-period expansion), validated directly on real survival::flchain. The robust GPU path is CUDA: backend='gpu_fp64' is exact (~1e-13 vs CPU) at every granularity, and backend='gpu' (float32) converges across the whole flchain sweep including monthly (~1e-5). On Apple Silicon (MPS, no float64) float32 is improved as of 4.2.3 (the premature-stop false negative is fixed) and converges on most grids, but remains intermittently fragile -- the float32 XtWX is not always positive-definite, so some sizes fail loud (A6-correct). For guaranteed large-scale GPU fits use CUDA gpu_fp64 or the CPU backend. Genuinely float32-infeasible designs (cond*eps_fp32 above the acceptance gate) fail loud on every backend.
- The person-period expansion (pystatistics/survival/_discrete.py) is a pure-Python double loop, but it is a small fraction of end-to-end discrete_time at scale (~1.7% at the monthly ~1M-row design); the binomial GLM fit dominates, which is why the GPU speedup is reported on the fit. As of 4.2.2 the CPU fit itself meets/beats R at scale (monthly 58s vs R glm 71s); the GPU (CUDA) is faster still (~4s gpu_fp64).
- Only the Efron tie handling (R's default) is tabulated in the head-to-head; Breslow is implemented but not separately validated here.
- Tiny real datasets (lung, n~227) sit near R's timer resolution for KM and log-rank; the synthetic scaling study, not the lung timings, carries the rigorous performance claim.

## 7. Which version of pystatistics do these results apply to?

- **Library version:** 4.2.3  ·  **Install source:** pypi
- **Frozen:** 2026-06-28  ·  **Reference:** R survival::survfit / survdiff / coxph
- **Hosts / devices:** **powerhouse** — Apple Silicon / Metal (MPS); **forge** — NVIDIA CUDA GPU; **r** — reference
- **Provenance:** Generated by drivers/survival on the pystatsval harness against a PyPI-installed pystatistics==4.2.3. Two library changes since 4.2.1 reshaped the discrete-time CPU and GPU performance, both validated here on the real survival::flchain person-period design: 4.2.2 rewrote the CPU least-squares core (qr_solve) to a single-pass rank-revealing QR matching R's lm.fit/glm.fit (CPU OLS/GLM now meet/beat R), and 4.2.3 fixed a float32 GPU premature-stop false-negative (the IRLS loop stopped at the float32 round-off floor while slow low-hazard interval dummies were still moving). Correctness vs R is unchanged (round-off). Hosts: powerhouse (Apple Silicon, CPU fp64 + Metal/MPS fp32, R 4.5.2) and forge (NVIDIA CUDA, CPU fp64 + CUDA fp32/fp64).

---
_Rendered 2026-06-28 00:00 UTC from `artifacts/survival/v4.2.3/manifest.json`._
