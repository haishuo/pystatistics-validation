# mvnmle + mice — 6.0.0 brutal red-team findings (handoff)

Fresh adversarial red-team (multi-agent, 16 attack surfaces, independent skeptic verification), **all 12 confirmed defects independently re-reproduced by hand**. This is the answer to "did these get the same depth as the other modules?" — **no; a genuinely adversarial pass finds real defects the 4.6.13 confirmatory validation missed**, including two GPU silent-wrongs that contradict the 5.0.0 reports' no-silent-wrong proofs.

Retracted: an apparent EM-hang (9-min stall during the run) did NOT reproduce in isolation — it was 16-way concurrency contention, not a liveness defect.

Dismissed by verification (6, correct behavior): fp32 accuracy-floor cases, unbounded-likelihood degeneracy handled loudly, MPS run-to-run non-determinism (disclosed), the deliberate R-matching loglik constant, and a bundled-dataset non-convergence at default max_iter.


## Confirmed defects (most severe first)

### 1. [MAJOR] Constant-column guard rejects a large-magnitude column that genuinely varies in fp64, claiming 'no interior MLE exists' when the MLE demonstrably exists
- **Module/surface:** mvnmle / conditioning-boundary
- **Input:** p=2, n=150. col0 = 1e8 + N(0,1)*1e-3 (150/150 distinct fp64 values, ptp~5.7e-3, variance ~1e-6, relative spread ~1e-11 which is ~67000 fp64 ulps of real spread); col1 = standard normal, independent. rng=default_rng(777).
- **Observed:** Default mlest raises SingularMatrixError: 'column(s) [0] have (near-)constant observed values (zero variance), so no interior maximum-likelihood estimate exists — the observed-data log-likelihood is unbounded as the fitted variance approaches zero'. But force=True yields a finite loglik and sigmahat[0,0] equal to the empirical variance to ratio 1.0000 — the MLE exists and is found. The column has 150 distinct values.
- **Why a defect:** The guard uses a fixed relative range test (rtol=1e-10 against scale=max|obs|), so any large-magnitude column with relative spread below 1e-10 is rejected as 'constant' even though it varies and has a well-defined MLE (proven by force=True). This is the exactly-in-scope 'near-constant (not exactly constant) column near the zero-variance guard' firing too early with a false rationale. Distinct from the correct case of an actually-identical-valued column, for which no MLE exists.
- **Verifier:** Reproduced exactly. Default mlest(X) raises SingularMatrixError claiming col0 has 'zero variance' and 'no interior MLE exists — loglik unbounded as variance→0'. This rationale is factually false: col0 has 150/150 distinct fp64 values and a real, representable variance of 1.1e-6 (~340k ulps of spread, well above the fp64 floor). force=True finds the MLE and matches the empirical variance to ratio 0.9999999999 with finite loglik 886.06.

Decisive proof it is a well-posed problem the guard wrongly rejects: (1) The Gaussian covariance MLE is translation-invariant. Centering col0 (subtract 1e8) fits with converged=True and yields a BIT-IDENTICAL sigmahat (max abs diff 1.1e-16) and identical logli

### 2. [MAJOR] fp32/MPS backend silently accepts (converged=True, no warning, no refuse) a non-stationary fit ~1100 nats below the fp64 optimum on near-collinear data that the fp64 CPU backend correctly refuses with SingularMatrixError
- **Module/surface:** mvnmle / fp32-classifier-attack
- **Input:** Near-collinear complete-data MVN: p=5, n=200, true covariance condition number 1e8 (eigenvalues logspace(0,-8)), seed=20260723. Reproduces across seeds 20260723/20260725 and p=5,10 at cond>=1e7-1e8 (drill probe: gaps 940-1965 nats, all with gpu converged=True/no-warn while cpu raised or converged=False). Well-posed data (cond<=1e4) is clean: gaps ~0, no false accepts.
- **Observed:** gpu: converged=True, .warnings=(), no Python warnings. cpu: raises SingularMatrixError (correlation-matrix minimum eigenvalue below tolerance). fp64 re-score: gpu-accepted fit = 3009.9 nats vs optimum 4121.9 -> optimality GAP = 1112 nats. Line interpolation toward the EM optimum raises fp64 loglik by +24 nats on the first 10pct step, proving the accepted point is not a local optimum (confirmed similarly at p=5 seed 20260725: gap 940, +19; p=10 seed 20260724: gap 1965, +49).
- **Why a defect:** Not the disclosed fp32 accuracy floor: the floor covers covariance ACCURACY, whereas here the classifier itself fails -- converged=True + empty warnings on a NON-stationary fit (interpolation is strictly uphill) that the fp64 sibling rejects with SingularMatrixError on the same input. Matches the task's flagged categories 'returns converged=True on a fit that is NOT a true stationary point' and 'succeeds quietly on input that SHOULD fail loudly'. cpu's refuse is the CORRECT contract (data is genuinely near-collinear), so the asymmetry is a real gpu-classifier gap, not R/cpu being wrong. Severity major not showstopper: confined to adversarial near-collinear conditioning (cond >= ~1e7-1e8); well-posed data is clean. But MPS-only users have no fp64 fallback, so this silent accept is their sole failure signal and it does not fire. Secondary corroboration on REAL GSS data (screening off, full n=75683): at p=24, cond(fitted)=4.4e3 the gpu path accepts converged=True/no-warn while cpu returns converged=False, gpu fit 113 nats below optimum -- same direction at far milder conditioning, though per-observation the gap is small.
- **Verifier:** Reproduced exactly on the given input (p=5, n=200, cond 9.4e7, seed 20260723): gpu fp32/MPS returns converged=True with .warnings=() and zero Python warnings, while the fp64 cpu sibling raises SingularMatrixError (corr min-eig 4.59e-08 < 1e-05) on identical data. fp64 re-score of the gpu fit = 3009.9 vs optimum 4121.9 → 1112-nat gap. I strengthened the reference beyond the candidate's EM force=True fit by using the CLOSED-FORM complete-data MLE (sample mean + 1/n sample covariance, no optimizer): loglik 4121.92, same 1112-nat gap, and the interpolation path from the gpu point toward this analytic MLE is monotonically strictly uphill at every step (t=0.02→+4.6 … t=1.0→+1112.0). This is unambi

### 3. [MAJOR] solver='reference' reports converged=True with a garbage, self-inconsistent loglik on full-rank but scale-disparate data; diverges from default far beyond fp64 round-off
- **Module/surface:** mvnmle / api-default-contract
- **Input:** n=300, p=4 MVN (seed default_rng(1)), full-rank moderate correlation, 10% MCAR; only the COLUMN SCALES are disparate: col0*=1e6, col3*=1e-6 (correlation matrix stays full-rank/well-conditioned). Also a monotone scale sweep col0*=s, col3/=s for s in 1e1..1e6.
- **Observed:** reference returns converged=True with reported loglik = -5.000e+19, while re-scoring its OWN returned muhat/sigmahat in an independent fp64 observed-data objective gives -3545 (the reported .loglik does not correspond to the returned parameters). The fit is ~2000 loglik units worse than the default path. In the scale sweep, reference already diverges from default by ~1500 loglik at s=1e5 (ll -2930 vs default -1422). The default forward-Cholesky path stays scale-robust (ll ~ -1420) across the whole sweep.
- **Why a defect:** Not the disclosed fp32 accuracy floor: this is the fp64 reference path, the correlation matrix is full-rank/well-conditioned (only units differ), and the default fp64 path solves it correctly. The failure is a false converged=True plus a reported loglik (-5e19) that contradicts the returned parameters (rescore -3545) — a silent-wrong + false-convergence contract violation, not gentle error growth. Caveat honestly stated: the extreme s=1e6 (a ~1e12 scale span) is at the edge of well-posed; the honest-but-wrong converged=False divergence already appears at the more plausible s=1e5.
- **Verifier:** Reproduced exactly. solver='reference' on the 1e12 column-scale-span input returns converged=True, loglik=-5.000e+19, and NO warnings, while its returned muhat/sigmahat are finite and PD (eigenvalues 1e-4..6.2e12) and independently rescore to a finite ~-3545 (batched) / -4530 (my own hand-written per-observation fp64 observed-data MVN loglik, no package code). So the reported .loglik does not correspond to the returned parameters, and convergence is falsely reported. Root cause is code-level, not the fp32 floor: CPUObjectiveFP64.compute_objective returns the sentinel 1e20 when any pattern's post-Givens Delta diagonal goes non-positive (_objectives/cpu.py:204); extract_parameters reports logl

### 4. [MAJOR] logreg silently collapses (and elsewhere wildly over-disperses) an imputed binary column under (quasi-)separation; ridge is n-times too weak and R mice gets it right
- **Module/surface:** mice / separation-collapse
- **Input:** n=600 binary target with a strong continuous predictor. Minority class = the 15 rows with the largest x1 (near-perfect separation), all kept observed; ~33% of the majority set to NaN. Observed minority rate 15/403 = 3.7%. column_kinds=['binary','numeric','numeric'], method='auto' (logreg), seed=0, m=20. Identical dataset fed to R mice 3.19.0 (method='logreg'). A sibling config (30 minority, milder predictor) exercises the opposite symptom.
- **Observed:** All 20 imputations x 197 missing rows collapse to category 0 (frac1 identically 0.0, between-imputation sd 0.0), identical on backend='cpu' and backend='gpu'(MPS), with zero warnings and empty .warnings. _fit_logistic returns beta_hat=[-906.7,+471.6,-3.2] (MLE diverged; ridge did not bound it) and point-estimate p=9.36e-14 for every missing row, so the eta clip pins all imputations to 0. In milder configs the SAME code path instead over-disperses: frac1 mean 0.11-0.36 with sd up to 0.30 (imputations that flip 90%+ of missing rows into the minority), again silently.
- **Why a defect:** Not a fail-loud guard (no error/warning at all) and not the fp32 floor (the collapse is deterministic and identical on CPU fp64). R is the correct reference here, not wrong: R mice bounds the fit with augment() and returns ~1.8%, while pystatistics returns 0% (or, in sibling configs, 36%). Root cause is a real code defect: methods/logreg.py:67 sets ridge = _RIDGE*diag_scale with NO n_obs factor, whereas the analogous polr slope ridge (methods/polr.py:81) uses _RIDGE*diag_scale*n_obs - about n times larger. The logreg ridge is orders of magnitude too weak to bound the logistic MLE or its posterior covariance (X'WX+ridge)^-1 under (quasi-)separation, so beta_hat saturates to |1e3| and the covariance blows up to ~1e5, giving either a deterministic collapse or garbage between-imputation dispersion. polr, with the n-scaled ridge, stayed stable across the same separation probes - confirming the asymmetry is the bug.
- **Verifier:** Reproduced exactly in the 6.0.0 venv. Isolated _fit_logistic returns beta_hat=[-906.7,471.6,-3.2] with cov diag ~[7.4e4,2e4,71] (MLE diverged, covariance blew up). Full mice() collapses the imputed binary column to frac1 mean=0.0000 sd=0.0000 with ZERO warnings and empty .warnings, IDENTICAL on backend=None (CPU fp64) and backend='gpu' (MPS) — so it is deterministic in fp64, not an fp32 artifact. The over-dispersion twin (30 minority, mild predictor) reproduces the opposite symptom silently: frac1 mean=0.106 sd=0.297 on CPU (ultra-sparse: 0.45 CPU / 0.37 MPS). R mice 3.19.0 logreg on the identical dataset gives frac1 mean=0.0165 sd=0.0122 (min 0.005, max 0.051) — a sensible conditional with 

### 5. [MAJOR] polyreg's convergence fallback misses ValidationError; high-cardinality nominal with many predictors crashes the entire mice() run
- **Module/surface:** mice / categorical-edges
- **Input:** MICEDesign with a 10-level nominal column (~14 obs/level) plus 20 fully-observed numeric predictors, n=140, 15% missing in the nominal column, column_kinds=['numeric']*20+['categorical']. At a sweep step the multinomial model needs (J-1)*p = 9*21 = 189 params but only 120 rows are observed.
- **Observed:** The whole mice() call dies with pystatistics.core.exceptions.ValidationError ('Insufficient observations: n=120 but model has 189 parameters. Need n > (J-1)*p = 189.') raised from the internal multinomial solver (_solver.py:139 via polyreg.py:56). All m imputations are lost; no partial result, no warning.
- **Why a defect:** Not a correct fail-loud guard: the dataset passes MICEDesign validation and the method has an explicit, documented fallback path for exactly this class of awkward mid-sweep fit -- it just fails to catch the exception type multinom actually raises. Trigger is realistic (wide survey data: a high-cardinality nominal item imputed from many other items drives (J-1)*p over n_obs even at modest missingness), and R's mice handles high-dimensional polyreg without aborting. The crash also leaks internal (J-1)*p implementation detail. One-line fix: add ValidationError to the caught tuple in polyreg.impute (and, for symmetry, polr). File: pystatistics/mice/methods/polyreg.py lines ~55-64.
- **Verifier:** Reproduced exactly. mice() on a well-posed wide-survey design (10-level nominal ~14 obs/level + 20 numeric predictors, n=140, 15% missing, passing MICEDesign validation) dies with pystatistics.core.exceptions.ValidationError ("Insufficient observations: n=120 but model has 189 parameters. Need n > (J-1)*p = 189."), raised at pystatistics/multinomial/_solver.py:140 via polyreg.impute. All m imputations are lost; no partial result.

Every claimed detail checks out against source:
- pystatistics/mice/methods/polyreg.py:56-64 catches only (ConvergenceError, NumericalError, np.linalg.LinAlgError). multinom raises ValidationError on under-identification (n <= (J-1)*p, _solver.py:137-143), which is

### 6. [MAJOR] Zero-variance observed column silently imputed as a zero-uncertainty constant (no warning; R mice flags it 'constant' and refuses)
- **Module/surface:** mice / missingness-pathology
- **Input:** A numeric column whose observed cells have no variance — the sharpest instance is a column with a SINGLE observed value and the rest NaN (here col index 1: 1 observed, 39 missing, in a 40x3 matrix). The sibling instance is a column with many observed cells that are all identical (e.g. 23 observed all == 3.0, 17 missing). Neither is a fully-missing column, so the existing all-NaN-column guard does not fire.
- **Observed:** mice() runs to completion with no warning and no logged event. Every imputation fills the 39 missing cells with the single observed value, so all 5 completed datasets are identical for that column: between-imputation variance B=0 and within-imputation variance 0. pool() then reports standard_error=0.0, riv=0.0, fmi(lambda_)=0.0 for a column that is 97.5% missing. The all-observed-equal sibling (n_obs=23, all==3.0) behaves identically: silent constant imputation, se=0, fmi=0, no warning.
- **Why a defect:** This is not R-being-wrong: R correctly flags and protects against the degeneracy; pystatistics is the one silently mishandling it. It is a genuine silent-wrong: the point 'imputation' (copying the only donor) is forced, but the propagated uncertainty (se=0, riv=0, fmi=0 on a 97.5%-missing column) is dangerously false, and the engine emits no warning at all. It is distinct from a correct fail-loud guard (the fully-missing-column and categorical <2-levels guards DO fire correctly here — this numeric zero-variance case simply has no guard). Reachable with a single degenerate column and no exotic conditioning.
- **Verifier:** Reproduced both instances on pystatistics 6.0.0 (venv600). Single-observed-value column (1 obs / 39 NaN, 97.5% missing): mice() completes, fills all 39 cells with the one donor value, r.warnings==(), and pool() returns se=0.0, riv=0.0, fmi=0.0. Sibling all-observed-equal column (23 obs all==3.0, 17 NaN): identical — silent constant fill, no warning, se=riv=fmi=0. R mice 3.19.0 on the same construction detects the degeneracy at it=0, logs loggedEvents(meth='constant', out='b'), sets the column method to '' (does NOT impute — leaves all 39 cells NA), and emits Warning 'Number of logged events: 1'. Source inspection of pystatistics/mice/design.py confirms guards fire for non-finite values, p<2-

### 7. [MAJOR] Degenerate within-variance (ubar=0): riv silently reported as 0, contradicting lambda_=1, and fmi silently wrong
- **Module/surface:** mice / rubin-edges
- **Input:** pool with all within-imputation variances exactly zero and non-zero between-imputation spread: estimates=[1,2,3,4], variances=[0,0,0,0], df_complete=100. (Also occurs per-column in 2D input, and independent of df_complete.)
- **Observed:** riv=0.0 while lambda_=1.0 in the SAME returned object (these must satisfy lambda=riv/(1+riv), so riv=0 implies lambda=0 — a self-contradiction). fmi=0.6667. ci_lower=ci_upper=nan. Estimate (2.5), standard_errors (1.4434) and total (2.0833) are all correctly computed and finite. No warning is emitted. R mice's convention for the same input gives riv=Inf and lambda=1 (consistent), correctly signalling that all variance is between-imputation.
- **Why a defect:** Root cause is pooling.py line 168: `riv = np.where(ubar > 0, (1+1/m)*between/ubar, 0.0)` substitutes 0 for the true +inf when ubar==0. This is silent-wrong, not a correct fail-loud guard: the pooled point estimate, SE, and total variance are computed correctly and returned without error, so the caller has every reason to trust the accompanying diagnostics — yet riv is 0 (should be +inf), it directly contradicts the simultaneously-returned lambda_=1, and fmi is derived from the wrong riv giving a plausible 0.6667 when the true fraction of missing information is ~1. The lambda_ path (line 169, guarded on total>0) correctly yields 1, proving the two diagnostics disagree. Degenerate within-variance is explicitly an in-scope edge; the contract that riv and lambda_ are consistent finite/sane values is violated with no warning.
- **Verifier:** Reproduced exactly with the venv: pool([1,2,3,4],[0,0,0,0],df_complete=100) returns riv=0.0, lambda_=1.0, fmi=0.6667, ci=(nan,nan), and 0 warnings, while estimate=2.5 and se=1.4434 are correct/finite. The internal contract lambda=riv/(1+riv) is violated: riv/(1+riv)=0.0 but lambda_=1.0. Root cause confirmed at pystatistics/mice/pooling.py:168 — riv=np.where(ubar>0,(1+1/m)*between/ubar,0.0) substitutes 0.0 for the true +inf when ubar==0, whereas line 169 computes lam guarded on total>0 and correctly yields 1.0; the two diagnostics come from different guards and contradict each other. fmi (line 172) is derived from the wrong riv, giving a plausible 0.6667 when the true fraction of missing info

### 8. [MAJOR] fp32 GPU (MPS) silently collapses binary/categorical/ordinal columns onto level 0 on a degenerate fit — the Rule-1 NaN sentinel is erased by indices_to_codes before the end-of-sweep guard sees it
- **Module/surface:** mice / fp32-nosilent-collapse
- **Input:** 3 columns: predictor x0 = N(0,1)*1e20 (extreme finite scale, NOT collinear/constant/rank-deficient), predictor x1 = N(0,1), and a discrete target with ~30% MCAR missing. Run three target kinds — binary (logreg), 3-level categorical (polyreg), 4-level ordered (polr) — via MICEDesign.from_array(..., column_kinds=[...]) and mice(des, seed=3, n_imputations=6, max_iter=4, backend='gpu') on MPS vs backend='cpu'. Reproduces for every seed and every predictor scale >= ~1e19 (where X'X overflows fp32's 3.4e38).
- **Observed:** On MPS fp32 all imputed cells of the discrete column collapse to the single lowest level (uniq=[0.] for binary/categorical/ordinal), with no NaN, no warning, no error. The GPU kernels DO emit the intended NaN fail-loud sentinel (verified: raw gpu_logreg_impute / gpu_polyreg_impute output is all-NaN), but the sweep's indices_to_codes casts NaN->0 and maps it to levels[0], a finite value that passes the end-of-sweep non-finite guard. The CPU backend returns a full spread ([0,1,2] etc.); for polr the CPU path even warns loudly (ConvergenceError -> marginal draw) on every step while the GPU path is silent.
- **Why a defect:** Not the fp32 accuracy floor and not a correctly-firing guard: the input is finite, full-rank, non-collinear, non-constant (only badly scaled), and CPU/fp64 handles it fine — so it is a well-posed problem on which cpu and gpu disagree by total-variation ~1.0 (all mass on one category vs a 3-way spread). It defeats the exact Rule-1 mechanism the samplers were written for: issue #8's original bug was categorical collapse-to-category-0; the fix makes the samplers emit NaN 'so a degenerate fit reaches the backend's end-of-sweep guard' (their docstrings), but indices_to_codes(NaN.long())=levels[0] erases the sentinel, re-introducing the silent collapse for precisely binary/categorical/ordinal targets while numeric targets still fail loud.
- **Verifier:** Reproduced deterministically on MPS fp32 for binary/categorical/ordinal targets: all imputed cells collapse to level 0 (uniq=[0.], nanfrac 0.0, no warning/error) while CPU returns a full spread. I confirmed the full mechanism against the live code: (1) the raw categorical GPU kernel emits ALL-NaN (nan_frac 1.0, zero finite indices) — the Rule-1 sentinel fires exactly as the sampler docstrings intend ("so a degenerate fit reaches the backend's end-of-sweep guard", _gpu_logreg.py:120, _gpu_polyreg.py:135, _gpu_polr.py:400); (2) indices_to_codes (_gpu_encode.py:88) is levels[indices.long()], and I verified NaN.long()==0 in torch, so every NaN is gathered to levels[0], a finite value; (3) the en

### 9. [MAJOR] GPU polyreg docstring claims a non-convergence marginal-draw fallback that does not exist in code, and unlike CPU the GPU categorical path emits no warning on a non-converged fit
- **Module/surface:** mice / fp32-nosilent-collapse
- **Input:** Same 1e20-scale predictor with an ordered/categorical target. The CPU polr path emits 'polr fit did not converge (ConvergenceError); using a marginal draw for this sweep step.' on every step while the GPU path is silent; _gpu_polyreg.py's module docstring states 'a marginal-distribution fallback covers a non-converged fit' though batched_multinomial_newton has no such branch.
- **Observed:** GPU categorical/ordinal fits that fail to converge produce no warning and no marginal-draw fallback; the docstring asserts a fallback that is not implemented.
- **Why a defect:** Documented-contract / behavior mismatch: the GPU categorical path is silently less safe than the CPU reference it claims parity with, and the code contradicts its own docstring — the same root cause that lets the primary silent collapse pass unnoticed.
- **Verifier:** Reproduced on MPS/fp32 (seed=12345, 3-level target quasi-separated by a 1e20-scale predictor). On the same fit, CPU polyreg/polr emit 12 'fit did not converge; using a marginal draw' warnings and impute a proper spread uniq=[0,1,2]; the GPU path emits ZERO warnings and collapses to uniq=[0.] (all missing rows -> category 0). This is a genuine silent-wrong outcome and a fail-loud-contract violation: (1) the imputations are FINITE, so the GPU backend's only safety net — the end-of-sweep non-finite guard in gpu.py (raises ValidationError) — never fires; it catches NaN/Inf, not a finite degenerate collapse (the documented 'issue #8' |alpha|~1e6 all-one-category fit). (2) _gpu_polyreg.py line 33 

### 10. [MINOR] gpu backend rejects well-posed, full-rank, merely-rescaled data that cpu fits exactly, with a SingularMatrixError that falsely blames the input for being collinear
- **Module/surface:** mvnmle / conditioning-boundary
- **Input:** n=800 draws from a 3-var MVN with correlation min-eigenvalue 0.373 (full-rank, NOT collinear), columns then multiplied by (1e-3, 1, 1e3) so cov condition number is ~1.66e12 but the scale-invariant correlation is unchanged. seed=np.random.default_rng(7).
- **Observed:** cpu returns converged=True with rel error 7.6e-9 vs the closed-form sample-covariance MLE (exact). gpu raises SingularMatrixError: 'the fitted covariance is rank-deficient (correlation-matrix minimum eigenvalue 1.62e-16 < 1e-05). The input has (near-)collinear variables, so no interior maximum-likelihood estimate exists'. Deterministic across runs. At scale exp 1e-4 gpu instead raises NumericalError (which correctly advises scaling); the two gpu failure modes are themselves inconsistent.
- **Why a defect:** Not the disclosed fp32 accuracy floor (that is a growing numeric gap, not raise-vs-solve) and not a correct fail-loud guard: the data is full-rank (corr min-eig 0.373), cpu recovers the exact MLE, and the SingularMatrixError misattributes an fp32 backend limitation to non-existent input collinearity, sending a user to hunt for collinear columns that aren't there. The module's own _degeneracy.py explicitly argues a raw covariance condition number cannot distinguish scaling from collinearity, yet the gpu_fp32 path keys on exactly that (get_initial_parameters: cond>1e10).
- **Verifier:** Reproduced exactly on this Mac (MPS fp32). cpu returns converged=True, relerr 7.6e-9 vs the closed-form sample-covariance MLE; gpu raises SingularMatrixError claiming "the input has (near-)collinear variables, so no interior MLE exists" (corr min-eig 1.62e-16). The input is provably well-posed and full-rank: true and sample correlation min-eig are both 0.373, cpu recovers the exact MLE, and — decisively — rescaling the columns (X/std) makes the SAME data fit on gpu to converged=True, relerr 2.2e-16, fitted corr min-eig 0.373. So the SingularMatrixError's two factual claims (input collinear; no MLE exists) are false, and its remedy ("remove the collinear columns") is wrong and destructive; th

### 11. [MINOR] gpu backend leaks a raw numpy.linalg.LinAlgError instead of the documented SingularMatrixError on extreme collinearity
- **Module/surface:** mvnmle / conditioning-boundary
- **Input:** Bivariate n=500 MVN with rho=0.999999 (fitted correlation min-eigenvalue ~9e-7, genuinely rank-deficient). rng=default_rng(2024) advanced through rho in [0.999,0.9999,0.99999,0.999995,0.999999] so the last X is used.
- **Observed:** gpu raises numpy.linalg.LinAlgError('batched Cholesky failed for 1/1 patterns after ridge escalation to 1.00e-01') from _batched_cholesky.py during objective evaluation, BEFORE the degeneracy guard. cpu on the identical X raises the documented SingularMatrixError. Deterministic across repeated fresh-process runs.
- **Why a defect:** A user who wraps mlest in `except SingularMatrixError` to handle collinear data (the documented contract, per the cpu path and the guard's own message) will have that handler bypassed on gpu and instead see an unwrapped LinAlgError. The input IS degenerate so failing loud is correct; the exception TYPE and backend inconsistency are the defect.
- **Verifier:** Reproduced deterministically in a fresh process (unique-named script; the shared-prefix file had been clobbered by a concurrent agent). On the identical rho=0.999999 X (fitted cov min-eig ~9.5e-7, genuinely rank-deficient): cpu raises pystatistics.core.exceptions.SingularMatrixError (documented), gpu raises numpy.linalg.LinAlgError (NOT in pystatistics.core.exceptions). Root cause is structural, not flaky: the rank-deficiency guard _guard_degeneracy runs post-fit at solvers.py:165 and its comment claims it covers 'every algorithm and backend', but the GPU objective's batched Cholesky raises a raw np.linalg.LinAlgError at _batched_cholesky.py:190 during optimization, before the guard can insp

### 12. [MINOR] Constant fully-observed categorical predictor is rejected while a constant numeric predictor is accepted; error message is misleading
- **Module/surface:** mice / categorical-edges
- **Input:** MICEDesign with two columns: an incomplete numeric target and a fully-observed single-level categorical predictor (constant value 3.0, no missing cells), column_kinds=['numeric','categorical'].
- **Observed:** The categorical case raises ValidationError: "Column 'x1' is 'categorical' but has only 1 observed level; need at least 2 to impute." The identical constant column declared 'numeric' is accepted and mice() completes.
- **Why a defect:** A false-positive rejection, not a correct fail-loud: the input is well-posed (a constant categorical covariate contributes nothing but is not invalid, and the analogous numeric case is accepted), and the diagnostic references imputation of a fully-observed column that is never imputed. Low severity because a constant column is degenerate and easily worked around, but the numeric/categorical asymmetry plus the inaccurate message is a real contract wart.
- **Verifier:** Reproduced exactly: a fully-observed single-level categorical predictor is rejected with "need at least 2 to impute", while the byte-identical column declared numeric is accepted and mice() completes. Root cause is design.py:_resolve_levels lines 363-367 — the uniq.size<2 guard runs unconditionally over all categorical columns with no missingness check, even though a fully-observed column is never imputed (_resolve_methods assigns it '' and incomplete_columns excludes it). This is a false positive, not the correct "constant column" fail-loud: (1) the identical numeric constant is silently accepted, so the library has no general reject-constant-columns contract — the treatment is asymmetric; 
