[project]
name = "pystatistics-validation"
version = "0.1.0"
description = "Canonical version-pinned validation evidence and reports for pystatistics"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
