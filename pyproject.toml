[tool.pytest.ini_options]
testpaths = ["suite1_pystatistics", "suite2_pystatsbio", "suite3_gpu_stress"]
addopts = "-v --tb=short -m 'not slow'"
markers = [
    "slow: large/slow tests (GPU stress). Run with: pytest -m slow",
    "gpu: requires CUDA GPU. Auto-skipped without one.",
]
